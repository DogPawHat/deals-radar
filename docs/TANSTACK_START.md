you can use the tanstack mcp to look up the tanstack react start docs for any questions you have.
It is important that you run `pnpm build` to regenerate the `routeTree.gen.ts` file when route changes are made.
