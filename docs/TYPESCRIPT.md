NEVER use `any` or `as any`. Prefer infered types over explicit types. Always try to preserve type information, fall back to `unknown` if that fails.
Use the path alias `@/*` for files in the `src` dir
Prefer named exports
